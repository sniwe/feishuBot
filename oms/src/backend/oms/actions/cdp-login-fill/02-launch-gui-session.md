# Step 02 - Launch GUI Session

Goal: start a visible OMS browser session.

Action:
- Run from workspace root:
  - `npm run launch:oms`

Expected behavior:
- Browser window opens in GUI mode.
- Launcher keeps running (long-lived process).
