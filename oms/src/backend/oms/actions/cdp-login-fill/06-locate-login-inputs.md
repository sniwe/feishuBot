# Step 06 - Locate Login Inputs

Goal: find username and password input elements.

Primary selectors:
- `input[placeholder="请输入账号名"]`
- `input[placeholder="请输入密码"]`

Fallback selectors:
- `input[name="username"]`
- `input[name="password"]`

Pass criteria:
- Both inputs are found and visible.
