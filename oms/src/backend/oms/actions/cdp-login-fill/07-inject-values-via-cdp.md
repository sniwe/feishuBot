# Step 07 - Inject Values via CDP

Goal: set username/password into the login form.

Action:
- Focus each field.
- Clear current value.
- Type values from config through the attached CDP session.

Notes:
- Use input/change events or typed keystrokes to match UI framework behavior.
